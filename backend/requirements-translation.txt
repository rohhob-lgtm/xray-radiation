# Lean dependency set for the standalone Translation Studio clone.
#
# Excluded vs. the platform's requirements.txt (unused by the translation
# feature — they belong to the removed ColPali visual RAG, web-crawler research
# agent, and NAS/SFTP connectors, verified: imported only by colpali_service,
# image_classifier, web_crawler):
#   torch, colpali-engine, open-clip-torch, playwright, asyncssh, smbprotocol
# Also excluded: weasyprint (not imported anywhere; PDF build uses reportlab,
# and weasyprint needs GTK system libs on Windows).
#
# Regenerate the full set from requirements.txt if you re-enable those features.

fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12
httpx==0.27.2
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1
Pillow==11.0.0
openai==1.54.4
anthropic>=0.40.0
aiofiles==24.1.0
# Database
sqlalchemy>=2.0.36
psycopg2-binary>=2.9.9
# Auth
authlib>=1.3.0
itsdangerous>=2.1.2
numpy>=1.24.0
# Document parsing
pypdf>=4.0.0
pymupdf>=1.24.0
pytesseract>=0.3.10
langdetect>=1.0.9
# Document build / rebuild
python-docx>=1.1.0
python-pptx>=0.6.23
reportlab>=4.0.0
matplotlib>=3.8.0
arabic-reshaper>=3.0.0
python-bidi>=0.4.2
openpyxl>=3.1.0
pyyaml>=6.0.0
fonttools>=4.50.0
# HTML parsing (document extraction)
beautifulsoup4>=4.12.0
lxml>=5.0.0
python-slugify>=8.0.0
# Microsoft Word/PowerPoint desktop COM (Windows-only, high-fidelity finalize).
# On Linux, LibreOffice headless is used instead (see libreoffice_finalizer.py).
pywin32>=306; sys_platform == "win32"
psutil>=5.9.0
# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
