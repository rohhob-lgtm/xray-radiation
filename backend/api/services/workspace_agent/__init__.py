"""AI Chat Workspace agent — Gemini function-calling loop over a per-user
file/folder workspace, plus the document generation/translation tools it
calls. Entered from api.routes.chat when a chat turn carries a workspace_id.
"""
